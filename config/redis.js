// const redis = require('redis');
// const client = redis.createClient({
//   host: process.env.REDIS_HOST || 'redis',  // Default to 'redis'
//   port: process.env.REDIS_PORT || 6379,
// });

// client.on('error', (err) => console.error('Redis Client Error', err));

// client.connect();
